# 🤝 Руководство по участию в проекте

Спасибо за интерес к улучшению Technology Tracker! Мы приветствуем любой вклад в развитие проекта.

## 🚀 Быстрый старт для разработчиков

### Настройка окружения
```bash
# Клонируйте репозиторий
git clone https://github.com/YOUR_USERNAME/technology-tracker.git
cd technology-tracker

# Установите зависимости
npm install

# Запустите в режиме разработки
npm run dev
```

## 📋 Как внести изменения

### 1. Создайте Issue
Перед началом работы создайте Issue с описанием:
- Что вы хотите добавить/исправить
- Почему это важно
- Как планируете реализовать

### 2. Форкните и создайте ветку
```bash
# Форкните репозиторий через GitHub UI
# Клонируйте свой форк
git clone https://github.com/YOUR_USERNAME/technology-tracker.git

# Создайте ветку для фичи
git checkout -b feature/amazing-feature
```

### 3. Разработка
- Следуйте существующему стилю кода
- Добавляйте комментарии для сложной логики
- Тестируйте изменения в браузере

### 4. Коммиты
Используйте конвенцию коммитов:
```bash
git commit -m "✨ feat: добавлена новая фича"
git commit -m "🐛 fix: исправлена ошибка импорта"
git commit -m "📚 docs: обновлена документация"
git commit -m "🎨 style: улучшены стили"
git commit -m "♻️ refactor: рефакторинг компонента"
```

### 5. Pull Request
- Опишите изменения подробно
- Приложите скриншоты если есть UI изменения
- Убедитесь что код работает

## 🎯 Области для улучшений

### 🔥 Приоритетные задачи
- [ ] Добавить темы (светлая/темная)
- [ ] Интеграция с реальным API
- [ ] Уведомления о дедлайнах
- [ ] Экспорт в PDF/CSV
- [ ] Мобильное приложение

### 🛠 Технические улучшения
- [ ] Добавить TypeScript
- [ ] Unit тесты (Jest/Vitest)
- [ ] E2E тесты (Playwright)
- [ ] Storybook для компонентов
- [ ] PWA функциональность

### 🎨 UI/UX улучшения
- [ ] Анимации переходов
- [ ] Drag & Drop для сортировки
- [ ] Кастомизация интерфейса
- [ ] Горячие клавиши
- [ ] Офлайн режим

## 📝 Стандарты кода

### React компоненты
```jsx
// Используйте функциональные компоненты
const MyComponent = ({ prop1, prop2 }) => {
  // Хуки в начале
  const [state, setState] = useState();
  
  // Обработчики событий
  const handleClick = () => {
    // логика
  };
  
  return (
    <div className="my-component">
      {/* JSX */}
    </div>
  );
};

export default MyComponent;
```

### CSS стили
```css
/* Используйте CSS переменные */
.component {
  background: var(--bg-card);
  border-radius: var(--radius-lg);
}

/* BEM методология для классов */
.card__header {}
.card__title {}
.card__title--active {}
```

### Хуки
```js
// Кастомные хуки начинаются с 'use'
export const useCustomHook = () => {
  // логика хука
  return { data, loading, error };
};
```

## 🐛 Сообщение об ошибках

При обнаружении багов создайте Issue с:
- **Описанием** проблемы
- **Шагами** для воспроизведения
- **Ожидаемым** поведением
- **Скриншотами** (если применимо)
- **Информацией** о браузере/ОС

## 💡 Предложения фич

Для новых идей создайте Issue с тегом `enhancement`:
- Опишите проблему которую решает фича
- Предложите решение
- Рассмотрите альтернативы
- Добавьте мокапы если есть

## 📞 Связь

- **Issues**: для багов и предложений
- **Discussions**: для вопросов и идей
- **Email**: [ваш email] для приватных вопросов

---

**Спасибо за вклад в развитие проекта! 🚀**